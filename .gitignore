# Cargo
target/
# Note: Cargo.lock IS committed for this crate (Node addon is a
# binary-shaped distribution; we need deterministic builds so CI and
# consumers resolve the same `rgb-lib` revision, which is what fixes
# the otherwise-conflicting rgb-invoicing rc pin between the parent
# rgb-lightning-node and Roman's lightning-invoice fork).

# Native addon prebuilds — distributed via GitHub Releases
*.node

# Node
node_modules/

# Generated by napi-rs
index.d.ts.bk

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
